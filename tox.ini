[tox]
envlist =
    py27,
    py33,
    py34,
    docs

[testenv]
deps=
    -rrequirements.txt
    pytest
    pytest-cov
    pytest-flakes
    pytest-pep8

commands=
    py.test rancidcmd/tests -v --pep8 --cov rancidcmd --cov-report html

[testenv:pylint]
deps=
    pylint
commands =
    pylint pytmpl --rcfile={toxinidir}/.pylintrc

[testenv:docs]
changedir = docs
commands =
    sphinx-build -b html -c source source build/html
