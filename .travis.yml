language: python
sudo: true
python:
- 2.7
- 3.3
- 3.4

before_script:
- sudo bash utils/test_setup.sh
- sudo service quagga status
- sudo ps aux | grep quagga
- /usr/lib/rancid/bin/clogin -t 10 -u "travis" -p "zebra" -e "zebra"  -c "show version" 127.0.0.1

install:
- pip install virtualenv tox coveralls

script:
- python setup.py test
- python setup.py check -r

after_success:
- coveralls
